<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devmob2018.com.pesquisaapp">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_directions"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DadosPessoaisActivity" />
        <activity android:name=".DadosContatoActivity" />
        <activity android:name=".DadosEnderecoActivity" />
        <activity android:name=".VisualizacaoActivity" />
        <activity android:name=".BaseActivity"></activity>
    </application>

</manifest>